# NexBusinessBrain – Environment Variables
# Copy this file to .env.local for local development
# Set these in Vercel Dashboard → Project → Settings → Environment Variables

# ─── Backend API ─────────────────────────────────────────────────────────────
# URL of the NexVision RAG backend (FastAPI / Python service)
# Leave empty to run in simulation mode (frontend only)
VITE_API_BASE_URL=https://your-backend-url.com

# ─── Feature Flags ───────────────────────────────────────────────────────────
# Set to "false" to connect to a real backend instead of simulating responses
VITE_USE_SIMULATION=true

# ─── App Meta ────────────────────────────────────────────────────────────────
VITE_APP_NAME=NexBusinessBrain
VITE_APP_VERSION=0.1.0
